# Microsoft Graph API Credentials (Required for Backend file uploads)
TENANT_ID=your_tenant_id_here
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
USER_EMAIL=your_onedrive_email_here

# Frontend Configuration (Optional)
# Leave empty for Vercel deployment, or set to http://localhost:5000 for local dev if needed
VITE_BACKEND_URL=
